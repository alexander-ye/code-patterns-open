---
problem_number: 21
title: Merge Two Sorted Lists Solution
src:
  {
    domain: LeetCode,
    url: https://leetcode.com/problems/merge-two-sorted-lists/,
    difficulty: "Easy",
  }
languages: [Python]
author: Alex Ye
---

## Problem Statement

<blockquote src={frontmatter.src_url}>

You are given the heads of two sorted linked lists `list1` and `list2`.

Merge the two lists into one **sorted** list. The list should be made by splicing together the nodes of the first two lists.

Return the _head_ of the merged linked list\_.

**Constraints**

- The number of nodes in both lists is in the range `[0, 50]`.
- <code>
    -100 {"<="} Node.val {"<="} 100
  </code>
- Both `list1` and `list2` are sorted in non-decreasing order.

<details>
<summary><strong>Example Test Cases</strong></summary>

**Example 1:**

```
Input: list1 = [1,2,4], list2 = [1,3,4]
Output: [1,1,2,3,4,4]
```

**Example 2:**

```
Input: list1 = [], list2 = []
Output: []
```

**Example 3:**

```
Input: list1 = [], list2 = [0]
Output: [0]
```

</details>
</blockquote>

## Definition of a Singly-Linked List

```python
# Definition for singly-linked list.
class ListNode:
  def __init__(self, val=0, next=None):
    self.val = val
    self.next = next
```

## Solution

```python

class Solution:
  def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
    out = ListNode()
    current = out
    l1node = list1
    l2node = list2
    while l1node and l2node:
      if l1node.val < l2node.val:
        current.next = l1node
        current = l1node
        l1node = l1node.next
      else:
        current.next = l2node
        current = l2node
        l2node = l2node.next
    if l1node or l2node:
      current.next = l1node if l1node else l2node

    return out.next
```
